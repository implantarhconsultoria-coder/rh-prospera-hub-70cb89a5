# Nota técnica

O novo Almoxarifado reaproveita a RPC `almoxarifado_criar_carga_v2` já usada pela versão anterior para preservar a baixa transacional de estoque. A mudança de interface elimina caminhos duplicados sem criar uma segunda regra de estoque.

name: Corrigir datas do aviso de ferias

on:
  push:
    paths:
      - '.github/workflows/fix-ferias-date-only.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Corrigir datas locais e retorno
        shell: bash
        run: |
          python3
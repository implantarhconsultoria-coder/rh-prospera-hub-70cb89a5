name: Padronizar aviso de estimativa nas rescisoes

on:
  push:
    paths:
      - '.github/workflows/patch-rescisao-auto-summary.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Aplicar aviso padrao em tela email e PDF
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib
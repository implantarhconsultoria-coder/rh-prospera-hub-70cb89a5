name: TOPAC Signature Digital Cleanup

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/topac-signature-page-cleanup.yml'

permissions:
  contents: write

jobs:
  rollout:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Apply role-based signature eligibility
        run: python scripts/topac_signature_eligibility_role_only.py

      - name: Validate production build
        run: |
          npm install --legacy-peer-deps --package-lock=false
          npm run build

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/payroll/PayrollPortalAdminModule.tsx src/components/payroll/BenefitSignatureGenerator.tsx api/payroll-public.ts api/payroll-archive.ts
          git commit -m "refactor: excluir socios da assinatura pela funcao"
          git push origin HEAD:main
